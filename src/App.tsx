import { Button } from "../lib/main";

function App() {
  return (
    <div style={{ maxWidth: 800, padding: 20 }}>
      <Button variant="primary" size="full">
        Login
      </Button>
    </div>
  );
}

export default App;
