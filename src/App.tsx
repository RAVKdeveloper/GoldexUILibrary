import { TextField } from "../lib/main";

function App() {
  return (
    <div style={{ padding: 20, background: "#343741" }}>
      <TextField variant="select" placeholder="Select" sizeType="full" />
    </div>
  );
}

export default App;
